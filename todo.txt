$participantManager = new ParticipantManager($pdo);

$participantId = 5; // Example participant ID
$confirmed = $participantManager->markConfirmationSent($participantId);

if ($confirmed) {
    echo "Confirmation marked as sent!";
} else {
    echo "Failed to update confirmation status.";
}





--
IS ACTIVE Doesne't work with standalone pages (like contact for instance)

MAKE SURE total_reimbursed is in the DB!!